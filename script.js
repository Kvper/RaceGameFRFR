var mode;

class car{
  
  constructor(x, y){
  this.x = x
  this.y = y
  this.speed = 0
  }

start(speed) { 
    this.speed = speed;
  }

  move() {
    function keyPressed(){
      if 
    
    }

    
    this.x += this.speed;
    if keyPressed {
      this.x = width;
    } else if (this.x > width) {
      this.x = -20;
    }
}

function setup() {
	createCanvas(600, 400);
  mode = 0;
  textSize(21);
  desley = new car (200, 100);
  desley.display();
  desley.move();
  
}

function draw(){
 
  }
  
  

